# How to run the applications?

## Clone the repository

### For backend (mongodb must be installed locally)
```bash
cd backend
yarn install
yarn dev
```

### For frontend
```bash
cd frontend
yarn install
yarn dev
```